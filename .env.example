# Copy to .env and fill in. On Unity, prefer exporting these in the sbatch script.
HF_TOKEN=

# Optional overrides (defaults live in src/config.py):
# WORDLE_QUICK=0
# WORDLE_BASE_MODEL=Qwen/Qwen2.5-7B-Instruct
# WORDLE_OUTPUT_ROOT=outputs
# WORDLE_USE_VLLM=0
# HF_HOME=/work/<pi>/<user>/hf_cache
